# SmartService — الربط الفعلي

تم توحيد الموقع مع مشروع ASP.NET Core الموجود في هذا المجلد بدل إبقاء بيانات الواجهة ثابتة. تم استخدام النماذج الموجودة في المشروع نفسه (`ApplicationUser`, `Provider`, `Service`, `Category`, `ServiceRequest`, `Notification`, `SystemSettings`) وترك قاعدة البيانات الحالية ومigrations كما هي لأن الحقول المطلوبة للربط كانت موجودة مسبقًا.

## ما تم ربطه

تمت إضافة لوحة عميل حقيقية تعرض أعداد الطلبات حسب الحالة، الطلبات الأخيرة، والخدمات المقترحة من قاعدة البيانات. كما تمت إضافة صفحات طلبات العميل وتفاصيل الطلب والإشعارات مع تقييد الوصول حسب `CustomerId`.

تمت إضافة لوحة مزود خدمة حقيقية تعرض عدد الخدمات والطلبات والطلبات الجديدة والمكتملة، مع صفحات إدارة خدماته، رفع صور الخدمات، تفعيل وتعطيل الخدمة، واستقبال الطلبات وتغيير حالتها. عند تغيير حالة الطلب يتم إنشاء إشعار حقيقي للعميل.

تمت إضافة كتالوج عام للخدمات ومزودي الخدمة وتفاصيل كل عنصر، مع البحث حسب الخدمة أو المدينة، وزر طلب الخدمة الذي يوجّه المستخدم إلى تسجيل الدخول عند الحاجة.

تم تحديث التسجيل ليحفظ الاسم الكامل والعنوان والصورة الشخصية في `ApplicationUser`. كما تم تفعيل رفع شعار مزود الخدمة عند التقديم، وإنشاء إشعارات حقيقية للإدارة عند وصول طلب انضمام جديد.

## الأدوار والصلاحيات

الأدوار التي تُستخدم في المشروع هي `Admin`, `Manager`, `Provider`, و`Customer`. تمت حماية لوحات العميل والمزود والإدارة باستخدام `Authorize(Roles = ...)`. بعد تسجيل الدخول يتم التوجيه تلقائيًا إلى لوحة الدور المناسب، بينما يذهب المدير إلى لوحة الإدارة الحالية.

## التشغيل في Visual Studio

افتح ملف `SmartServiceManagement.sln` في Visual Studio، وتأكد من تشغيل SQL Server LocalDB أو تعديل `ConnectionStrings:BloggingDatabase` في `appsettings.json` إلى اتصال SQL Server لديك. بعد ذلك شغّل المشروع؛ سيقوم `Program.cs` بإنشاء الأدوار والحساب الإداري الافتراضي عند الحاجة.

بما أن التعديلات استخدمت الجداول والحقول الموجودة في migrations الحالية ولم تضف أعمدة جديدة، لا تحتاج إلى migration جديدة لهذه المرحلة. إذا تم تغيير أسماء الجداول أو إضافة حقول أخرى في المستقبل، نفّذ `Add-Migration` ثم `Update-Database` من Package Manager Console.

## ملاحظة أمان مهمة

يحتوي `appsettings.json` المرفق على كلمة مرور SMTP ظاهرة. يجب تغييرها أو نقلها إلى User Secrets / Environment Variables قبل رفع المشروع إلى GitHub أو نشره على استضافة عامة.
